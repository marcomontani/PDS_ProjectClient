<Window x:Class="PDS_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PDS_Client"
        mc:Ignorable="d"
        Title="BackupFolder" Height="350" Width="525">
    <Grid>
        <TabControl>
            <TabItem Header="La mia cartella" Height="22" VerticalAlignment="Bottom">
                <Grid Background="WhiteSmoke">


                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="202*"/>
                        <ColumnDefinition Width="6*"/>
                        <ColumnDefinition Width="133*"/>
                        <ColumnDefinition Width="170*"/>
                    </Grid.ColumnDefinitions>


                    <StackPanel Orientation="Vertical" Name="fs_grid" Grid.ColumnSpan="4">

                    </StackPanel>

                    <!--
                    <ListBox  Name="cwd" ItemsSource="{Binding}" />
                    -->
                </Grid>
            </TabItem>
            <TabItem Header="File Cancellati">
                <Grid Background="#FFE5E5E5">

                </Grid>
            </TabItem>
            <TabItem Header="Impostazioni">
                <Grid Background="WhiteSmoke">
                    <StackPanel Orientation="Horizontal" Name="vsp_settings">
                        <Label>Folder: "C:\Users\Marco\Documents\PDS_Folder" </Label>
                    </StackPanel>

                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
